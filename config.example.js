/* ============================================================
 *  config.example.js
 *  -------------------------------------------------------------
 *  这是 *模板* 文件。请复制为 config.js 并填入真实值。
 *  config.js 已加入 .gitignore,不会进入版本库。
 *
 *  生产环境 (Vercel) 部署时,本文件不会被使用 ——
 *  Vercel Build 阶段会运行 scripts/build-config.js,
 *  从环境变量自动生成 config.js,把密钥安全地注入到前端。
 * ============================================================ */

window.env = {
  SUPABASE_URL:      "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY",
};
